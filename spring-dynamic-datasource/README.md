# spring-dynamic-datasource

动态数据源切换示例：按配置注册多个数据源（dbsource1/dbsource2，均为内嵌 H2），运行时按 code 路由。

## 运行

无外部依赖。

```bash
./gradlew :spring-dynamic-datasource:bootRun
```

## 配套博客

[spring动态数据源](https://springcamp.cn/spring-dynamic-datasource/)
